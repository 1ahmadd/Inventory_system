# سجل التغييرات — نظام سجل الميدان لجرد شقق الأطباء

الإصدار: 2.0.0 — تاريخ التعديل: 2026-09-03

جميع التعديلات أُضيفت دون المساس بالتصميم الأصلي (الألوان، الخطوط، الهوية البصرية، نموذج طباعة A4) وبنية المشروع (React 19 + Vite + Express + Tailwind 4).

## أولاً — نظام الدخول الآمن (المستخدم وكلمة المرور + رمز التأكيد)

1. واجهة تسجيل دخول جديدة بالكامل (`client/src/pages/Login.tsx`) بنفس هوية التصميم: لا تُفتح أي واجهة من واجهات التطبيق إلا بعد اجتياز الدخول.
2. الدخول على مرحلتين: اسم مستخدم + كلمة مرور، ثم رمز تأكيد من 6 أرقام (OTP) صالح لمدة 10 دقائق وبحد أقصى 5 محاولات، ويُرسل إلى البريد المعتمد a77ahmadd@gmail.com عبر خدمة Gmail (nodemailer).
3. حساب المدير الافتراضي الذي أُضيف:
   - اسم المستخدم: `admin`
   - كلمة المرور: `Ahmed@2026`
4. كلمات المرور تُخزَّن مجزأة بخوارزمية scrypt مع Salt لكل مستخدم — لا تُحفظ كنص صريح أبداً.
5. الجلسات برمز Bearer عشوائي تُحفظ في `server/data/sessions.json` ومدتها 30 يوماً، مع زر «تسجيل خروج» في الشريط الجانبي والشريط العلوي.
6. ملفات الخادم الجديدة: `server/auth.ts` (واجهات REST للمصادقة)، وربطها في `server/index.ts` وفي خادم التطوير عبر `vite.config.ts`.

## ثانياً — واجهة إدارة المستخدمين والصلاحيات

1. واجهة جديدة «المستخدمون» (`client/src/pages/Users.tsx`) تظهر للمدير فقط، وتتضمن: إضافة مستخدم جديد (اسم مستخدم + كلمة مرور + صلاحية)، تعديل كلمة المرور لأي مستخدم، وحذف مستخدم (مع منع حذف الحساب الحالي أو آخر مدير).
2. الصلاحيات مقيدة بحساب المدير الافتراضي: واجهات إدارة المستخدمين (`GET/POST /api/users`، و`DELETE`) لا تعمل إلا برمز جلسة صادر من حساب بدور «admin»؛ المستخدم العادي يشغّل النظام فقط ويستطيع تغيير كلمة مروره الخاصة.
3. واجهة برمجية للمستخدم: `client/src/lib/authClient.ts`.

## ثالثاً — الفرز والتجميع حسب الفئات

1. دالة تجميع مشتركة `groupByCategory` تفرز القطع بترتيب الفئات المعتمد: أجهزة كهربائية، أثاث، مفروشات، أدوات مطبخ، صحيات، إلكترونيات، أخرى.
2. واجهة «سجل القطع»: أصبحت معروضة كمجموعات، كل فئة تحت شريط عنوان بلون الحبر الكحلي مع عدد سجلاتها وإجمالي كمياتها.
3. لوحة المتابعة: قسم جديد «توزيع القطع على الفئات» ببطاقات ملخصة لكل فئة.
4. نموذج طباعة A4: تُطبع القطع مجمعة بفئاتها، ويسبق كل مجموعة صف عنوان للفئة، مع ترقيم متسلسل عبر كامل المستند.

## رابعاً — دعم الجوال والتثبيت كتطبيق (PWA)

1. `client/public/manifest.json` محدّث: أيقونات 192/512 (عادية وmaskable)، وضع standalone، اتجاه RTL، وألوان الهوية.
2. عامل خدمة `client/public/sw.js` لتخزين هيكل التطبيق محلياً وتسريع الفتح، ويُسجَّل من `client/src/main.tsx` في وضع الإنتاج فقط (لا يؤثر على التطوير).
3. وسوم Apple/Android في `client/index.html` لتثبيت التطبيق من متصفح الجوال بأيقونة واسم على الشاشة الرئيسية.
4. تحسينات استجابة إضافية للجوال (شاشة الدخول، واجهة المستخدمين، شرائط الفئات) في `client/src/index.css` دون تغيير أي قاعدة أصلية.

## خامساً — الاستغناء عن التخزين الخارجي

- كانت الأصول البصرية تُحمَّل من خادم تخزين خارجي (/manus-storage) غير متاح خارج بيئة الإنشاء. استُبدلت بأصول محلية مولّدة داخل `client/public/assets/` (الشعار، صورة الحالة الفارغة، نسيج الورق، أيقونات PWA) ليعمل المشروع ذاتياً على أي استضافة.

## سادساً — ملفات أخرى

- `package.json`: إضافة nodemailer وأنواعه.
- `docs/دليل-الرفع-GitHub-Render.md`: دليل النشر خطوة بخطوة.
- `.env.example`: متغيرات البريد والحساب الافتراضي.
- `server/data/` مضافة إلى `.gitignore` (تحفظ المستخدمين والجلسات على الخادم فقط).
